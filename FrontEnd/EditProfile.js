window.onload = () => {
   // variables
   const change = document.getElementById("profilepic");
   
   // functions
   // go to edit profile page
   const change_pic = (e) => {
       // change profile picture
   }

   // calls
   change.addEventListener("click", change_pic);
}